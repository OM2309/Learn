#include <bits/stdc++.h>
using namespace std;

int main()
{
    // vector<int> arr;

    // cout << arr.size() << endl;
    // cout << arr.capacity() << endl;

    // arr.push_back(5);
    // arr.push_back(6);

    // for (int i = 0; i < arr.size(); i++)
    // {
    //     cout << arr[i] << " ";
    // }

    // cout << endl;

    // vector<int> brr(10);

    // for (int i = 0; i < brr.size(); i++)
    // {
    //     cout << brr[i] << " ";
    // }

    // cout << endl;

    // vector<int> brr(10, -1);

    // for (int i = 0; i < brr.size(); i++)
    // {
    //     cout << brr[i] << " ";
    // }

    // cout << endl;

    vector<int> brr{10, 20, 30, 40};

    for (int i = 0; i < brr.size(); i++)
    {
        cout << brr[i] << " ";
    }

    cout << endl;

    return 0;
}
